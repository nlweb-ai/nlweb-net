apiVersion: v1
kind: Service
metadata:
  name: nlwebnet-demo-service
  labels:
    app: nlwebnet-demo
spec:
  selector:
    app: nlwebnet-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP